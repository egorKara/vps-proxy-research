// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// GitHub Pages project site (username.github.io/<repo>/)
	base: '/vps-proxy-research',
	trailingSlash: 'always',
});
